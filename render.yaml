services:
  - type: web
    name: tamil-audio-translator
    runtime: python
    pythonVersion: "3.11"
    buildCommand: "echo 'No build needed'"
    startCommand: "python server.py"
    envVars:
      - key: PORT
        value: 10000
